import {
  useCopilotChat
} from "./chunk-QHCLJODF.mjs";

// src/hooks/use-copilot-chat.ts
function useCopilotChat2(options = {}) {
  const {
    visibleMessages,
    appendMessage,
    reloadMessages,
    stopGeneration,
    reset,
    isLoading,
    runChatCompletion,
    mcpServers,
    setMcpServers
  } = useCopilotChat(options);
  return {
    visibleMessages,
    appendMessage,
    reloadMessages,
    stopGeneration,
    reset,
    isLoading,
    runChatCompletion,
    mcpServers,
    setMcpServers
  };
}

export {
  useCopilotChat2 as useCopilotChat
};
//# sourceMappingURL=chunk-X7MJWPI7.mjs.map